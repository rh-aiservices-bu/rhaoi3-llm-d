apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - inference-service.yaml

patches:
  - target:
      kind: PodMonitor
      name: vllm-monitor
    patch: |-
      - op: replace
        path: /spec/selector/matchLabels/serving.kserve.io~1inferenceservice
        value: "qwen-vllm"
  - target:
      kind: Service
      name: vllm-lb
    patch: |-
      - op: replace
        path: /spec/selector/app
        value: "isvc.qwen-vllm-predictor"